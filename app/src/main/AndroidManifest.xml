<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 블루투스 권한 (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 구버전 안드로이드 및 비콘 수신용 위치 권한 (안드로이드 12+ 최신 버전 호환) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 포그라운드 서비스 및 알림 권한 (Android 13/14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- iOS 스타일 팝업 오버레이 권한 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/stat_sys_data_bluetooth"
        android:label="에어팟 매니저"
        android:roundIcon="@android:drawable/stat_sys_data_bluetooth"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- 메인 화면 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 에어팟 오픈 시 뜨는 iOS 스타일 팝업 액티비티 -->
        <activity
            android:name=".ui.popup.AirPodsPopupActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:exported="false" />

        <!-- 백그라운드 BLE 감시 포그라운드 서비스 -->
        <service
            android:name=".service.AirPodsMonitorService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />

    </application>

</manifest>
